$.ajax({
	type : 'GET',
	url : 'http://www.bnro.ro/nbrfxrates.xml#sthash.Zb5ZuVfe.dpuf',
	data : "xml",
	success:  function(xml){
		$(xml).find('cube date').each(function(){
		var rate = $(this).find('Rate').text();
		$("<li></li>").html(rate).appendTo("#currency");
 });
	}
})